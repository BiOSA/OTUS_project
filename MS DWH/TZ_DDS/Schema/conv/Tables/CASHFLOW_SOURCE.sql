CREATE TABLE conv.CASHFLOW_SOURCE
(
	  RK                BIGINT       NOT NULL IDENTITY(1, 1) CONSTRAINT PK_conv_CASHFLOW_SOURCE PRIMARY KEY
	, ID                SQL_VARIANT  NOT NULL
	, SOURCE_SYSTEM_CD  VARCHAR(3)   NOT NULL                CONSTRAINT FK_conv_CASHFLOW_SOURCE_dbo_SOURCE_SYSTEM FOREIGN KEY REFERENCES dbo.SOURCE_SYSTEM ON UPDATE CASCADE
	, [Source]          sysname          NULL
	                                                       , CONSTRAINT UX_conv_CASHFLOW_SOURCE_ID_SOURCE_SYSTEM_CD UNIQUE(ID, SOURCE_SYSTEM_CD) WITH (IGNORE_DUP_KEY = ON)
);
GO
CREATE INDEX IX_conv_CASHFLOW_SOURCE_ID      ON conv.CASHFLOW_SOURCE (ID);
GO
GRANT INSERT, DELETE, UPDATE, SELECT ON conv.CASHFLOW_SOURCE TO tz_report;